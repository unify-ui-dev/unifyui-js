{
  "name": "unoify-js",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "format":"@biomejs/biome format ./packages",
    "format-lint":"@biomejs/biome check --apply "
  },
  "devDependencies": {
    "@biomejs/biome": "1.4.1",
    "typescript": "^5.2.2",
    "vite": "^5.0.8",
    "vitest": "^1.1.1"
  }
}
